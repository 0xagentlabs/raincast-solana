# RainCast 项目使用说明书

## 项目简介

RainCast 是部署在 Solana Devnet 的天气预测市场 Demo。用户用测试 SOL 选择 YES/NO；截止后，授权预言机读取 Open‑Meteo 的上海实时降水量并签名提交，程序按 0.1 mm 阈值结算。赢家按获胜侧份额瓜分奖池；若获胜侧无人下注，所有参与者取回自己的本金。

该 Demo 的数据是真实天气 API 数据，但预言机发布者是单一受控签名者，并非完全去中心化。不可用于真实资金。

## 架构与目录

- `program/`：Rust + Pinocchio 链上程序。
- `app/`：Next.js App Router、Wallet Adapter 和显式 ABI 客户端。
- `oracle/settle.ts`：可选 CLI 结算工具；网页已内置运营方结算界面。
- `scripts/initialize.ts`：初始化 config PDA 与预言机公钥。
- `tests/`：TypeScript ABI/PDA 测试；Rust 单元测试位于程序源码。
- `ABI.md`：tag、二进制参数、账户顺序、PDA 和错误码。

## 环境与安装

需要 Rust、Solana CLI 4.x、`cargo-build-sbf`、Node.js 20+ 与 pnpm。仅配置 Devnet：

```bash
solana config set --url devnet
solana config get
solana address
solana balance
pnpm install --ignore-scripts
```

本地启动：`pnpm --dir app dev`，打开 `http://localhost:3000`。钱包必须切换至 Devnet。

## 链上地址

- Program ID：`BbkDnkPC7HD8TeNHp3iCDwjLxF3WDmg2Yrh9gVZrwohH`
- Config PDA：`AhRKMetrMa99zdnzuWmC5SnFFEJubB81ywdqdJGgrdnT`
- Oracle：`Dy6mBH4YeqJCRZohd39iSFaf4jyLaxPeBakbZwt1jToL`
- PDA：config=`[config]`；market=`[market, creator, market_id_le]`；position=`[position, market, bettor]`
- [Solana Explorer](https://explorer.solana.com/address/BbkDnkPC7HD8TeNHp3iCDwjLxF3WDmg2Yrh9gVZrwohH?cluster=devnet)

## 页面操作

1. 钱包切换至 Devnet并连接。
2. 点击“创建 5 分钟测试市场”；页面发送 CreateMarket，2 分钟后封盘、5 分钟后可结算。
3. 在市场卡片选择 YES 或 NO，每次投入 0.01 Devnet SOL。
4. 到达结算时间后，连接 Oracle 钱包 `Dy6m…jToL`；页面显示“已验证：运营方钱包”。
5. 点击“获取天气并签名结算”，核对钱包交易并签名；其他钱包无法通过合约权限校验。
6. 刷新市场，获胜用户点击“领取收益”。

完整参数和账户顺序以 `ABI.md` 为准。客户端在 `app/lib/program.ts` 显式编码，不使用 Anchor IDL。

## 天气数据与结算

预言机请求：

```text
https://api.open-meteo.com/v1/forecast?latitude=31.13&longitude=121.47&current=precipitation&timezone=Asia%2FShanghai
```

脚本校验 HTTP 状态、数值类型及范围，再以预言机密钥签名。合约校验 config PDA、oracle signer、市场状态、结算时间、观测时间不得来自未来且不得超过 48 小时。私钥文件在 `.gitignore` 中，禁止提交或打印。

CLI 备用方式：`ORACLE_KEYPAIR=/安全路径/operator.json pnpm oracle:settle -- <MARKET_PDA>`。网页结算不上传私钥，由钱包扩展直接签名。

## 测试、构建与部署

```bash
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test
cargo build-sbf
pnpm test
pnpm --dir app build
solana program deploy target/deploy/weather_market_program.so --program-id program-keypair.json --url devnet
pnpm run init
pnpm run oracle:set -- <NEW_ORACLE_PUBKEY>
```

## 查询和确认

交易成功后可使用 `solana confirm -v <SIGNATURE> --url devnet`，账户可在 Solana Explorer 查看。当前版本部署签名：`2r7vmr3dABmfEzSy6YTZWcUbrBbBQEaEnDZreDNTaB8zTLopRSbz7A5C8DxYhkfoia4NFUdXcmtmaConbgGeCjcB`；运营方更新签名：`48UBLK6zonKKMf3YAPry5czBppQFi69UBudw1DVgq9udRfQyJudFwJbEhi8vK9vEJmeMQeNzE4HVY4dVP4DyWn7F`。

## 常见错误

- `InvalidTime`：截止/结算时间不合法，或下注已经截止。
- `InvalidAuthority`：提交结算的钱包不是 config 中记录的 oracle。
- `ObservationStale`：未到结算时间，或观测时间过旧/来自未来。
- `NoWinnings`：当前钱包没有获胜份额。
- RPC 429：更换可靠的 Devnet RPC 后重试，不要切换至 mainnet-beta。

## 安全注意事项

链上会校验 signer、writable、owner、数据长度、PDA、时间和算术溢出。天气 API 本身与授权预言机仍是信任点；生产版本应使用多节点 Switchboard Feed、延迟/争议窗口、紧急暂停和审计后的资金托管模型。

## 项目链接

- GitHub：https://github.com/0xagentlabs/raincast-solana
- Vercel Production：https://raincast-solana-0xcevin-5020s-projects.vercel.app
- Solana Devnet：见上方 Explorer 链接
